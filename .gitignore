# Secrets
.env
.env.local
*.pem

# Personal data and generated documents
data/private/
artifacts/
out/
*.pdf
*.docx

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# LangGraph
.langgraph_api/

# OS / IDE
.DS_Store
.idea/
.vscode/
.neon

# Neon's own agent skills, installed per-directory by `neon skills`. Useful
# locally, but vendor documentation does not belong in this repository — and
# note they live here rather than in skills/, which is loaded into the agent.
.agents/
skills-lock.json
